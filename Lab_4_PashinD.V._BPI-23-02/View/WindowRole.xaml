<Window x:Class="Lab_4_PashinD.V._BPI_23_02.View.WindowRole"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_4_PashinD.V._BPI_23_02.View" 
        mc:Ignorable="d"
        Title="Должности" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Background="#FFE5E5E5" Orientation="Horizontal" Grid.Row="0">
            <Button x:Name ="AddRole" Content="Добавить" Margin="10,10,5,10" Command="{Binding AddRole}"/>
            <Button x:Name ="EditRole" Content="Редактировать" Margin="10,10,5,10" Command="{Binding EditRole}"/>
            <Button x:Name ="DeleteRole" Content="Удалить" Margin="10,10,5,10" Command="{Binding DeleteRole}"/>
        </StackPanel>
        <StackPanel Background="#FFE5E5E5" Grid.Row="1">
            <Label Margin="5" HorizontalAlignment="Center">Список должностей сотрудников</Label>
            <ListView x:Name="lvRole" SelectedItem="{Binding SelectedRole}">
                <ListView.View>
                    <GridView>
                        <GridView.Columns>
                            <GridViewColumn Header="Код" Width="50" DisplayMemberBinding="{Binding Id}"/>
                            <GridViewColumn Header="Наименование должности" DisplayMemberBinding="{Binding NameRole}"/>
                        </GridView.Columns>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
